version: '3.8'

services:
  postgres_gestao_vagas:
    container_name: gestao_vagas_db
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=gestao-vagas